/**
 * @param {string} s1
 * @param {string} s2
 * @return {boolean}
 */
var isScramble = function (s1, s2) {

};

function test() {
  let fun = isScramble;
  let params = [
    //[1, 1, 1, 0]
  ];

  const res = fun.apply(null, params);
  console.log(`res`, res);
}
test();
