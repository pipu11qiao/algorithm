/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function (s, p) {

};

function test() {
  let fun = isMatch;
  let params = [
    //[1, 1, 1, 0]
  ];

  const res = fun.apply(null, params);
  console.log(`res`, res);
}
test();
