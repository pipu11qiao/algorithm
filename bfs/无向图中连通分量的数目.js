/**
 * @param {number} n
 * @param {number[][]} edges
 * @return {number}
 */
var countComponents = function (n, edges) {

};

function test() {
  let fun = countComponents;
  let params = [
    [[0, 1], [1, 2], [3, 4]]
  ];


  const res = fun.apply(null, params)
  console.log(`res`, res);
}
test()